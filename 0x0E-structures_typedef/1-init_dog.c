#include "dog.h"
#include <stdlib.h>

/**
 * init_dog - function that gets the length of a prefix substring.
 * @d: The first string
 * @name: The string concatenates
 * @age: edad
 * @owner: duenio
 */
void init_dog(struct dog *d, char *name, float age, char *owner)
{
	if (d == NULL)
		
	else
	{
	d->name = name;
	d->age = age;
	d->owner = owner;
	}
}
