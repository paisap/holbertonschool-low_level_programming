/**
 * op_add - suma
 * @a: The first string
 * @b: the second string
 * Return: dest
 */
int op_add(int a, int b)
{
	return (a + b);
}

/**
 * op_sub - function that  concatenates two strings.
 * @a: The first string
 * @b: the second string
 * Return: dest
 */
int op_sub(int a, int b)
{
	return (a - b);
}

/**
 * op_mul - function that  concatenates two strings.
 * @a: The first string
 * @b: the second string
 * Return: dest
 */
int op_mul(int a, int b)
{
	return (a * b);
}

/**
 * op_div - function that  concatenates two strings.
 * @a: The first string
 * @b: the second string
 * Return: dest
 */
int op_div(int a, int b)
{
	return (a / b);
}

/**
 * op_mod - function that  concatenates two strings.
 * @a: The first string
 * @b: the second string
 * Return: dest
 */
int op_mod(int a, int b)
{
	return (a % b);
}
