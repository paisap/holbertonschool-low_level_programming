#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
* main - Entry point
*
* Return: Always 0 (Success)
*/

int main(void)
{
	int a, x = 2772;

	srand(time(NULL));
	a = 32 + rand() % (126 - 32);

	while (acu != 0)
	{
		printf("%c", num);
		x = x - a;
		a = 32 + rand() % (126 - 32);
		if (x < 126 && x > 32)
		{
			a = x;
		}
	}

	return (0);
}
