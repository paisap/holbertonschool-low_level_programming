#include <stdlib.h>
#include <stdio.h>
/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int main (int argc, char *argv[])
{
	int x = 0, r = 0, tr = 0, may;
	char *aux;

	aux = mallo(20);
	aux = argv[1];
	while (aux[x] != '\0')
		x++;
	aux = argv[2];
	while (aux[r] != '\0')
		r++;
	if (x >= y)
		may = x;
	else
		may = r;
	for (tr = 0; tr < may; tr++)

	printf("argc %d argv [0]: %s\n", argc, argv[0]);
	return(0);
}
